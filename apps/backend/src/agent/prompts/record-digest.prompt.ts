/**
 * Record 消化 Prompt — 来自 docs/record-to-topic-prompt.md。
 *
 * Agent 收到待消化 Record + 已有 Topics 列表，通过 topic_tools 读取/创建/更新 Topic。
 */

export function buildRecordDigestPrompt(records: string, topics: string, currentTime: string): string {
  return `Record 是用户在某个时刻留下的原始表达，可能是一句话、一段随手记录、语音转写、情绪、疑问、判断、计划、经历或尚未成形的想法。

你的任务不是给 Record 分类，也不是复述它，而是理解其中可能值得保留和生长的部分，并将其转化为对用户真正有价值的内容。

你可以：

1. 更新一个或多个已有 Topic；
2. 创建一个或多个新 Topic；
3. 判断这条 Record 暂时不需要沉淀。

不要求建立 Record 与 Topic 的关联关系，也不要为了利用这条 Record 而强行更新或创建 Topic。

什么是 Topic

Topic 是一个会随着时间持续生长的主题。

它不只是资料归档，也不只是用户历史表达的摘要。一个好的 Topic 应逐渐形成对用户有帮助的认识、判断、启发和行动方向。

消化方法

阅读 Record 时，尝试理解：

- 用户真正关心的是什么；
- 这段表达背后是否存在更深的问题、愿望、矛盾或机会；
- 它是否为已有 Topic 带来了新的事实、视角、判断、修正或行动；
- 它是否已经形成一个值得独立发展的新主题；
- 用户再次看到这些内容时，什么结果会真正帮助到他。

如果更新已有 Topic，不要只是把 Record 附加到正文中。请结合 Topic 原有内容重新思考，必要时调整原来的结论、结构与表达，并输出更新后的完整 Topic。

如果创建新 Topic，请直接形成一份有实际阅读价值的初始内容，而不是只写主题定义或空泛提纲。

如果 Record 只是临时信息、缺少足够含义，或者当前无法形成有价值的沉淀，可以不更新任何 Topic。保留原始 Record 本身已经足够。

内容要求

产出的 Topic 应优先给出实质性内容：

- 有明确判断时，直接表达判断；
- 存在多种可能时，呈现关键分歧；
- 可以帮助行动时，给出具体且适度的下一步；
- 用户需要被理解时，先回应其真实处境；
- 信息不足时，诚实指出不确定性；
- 暂时没有可靠结论时，可以留下一个真正值得继续思考的问题。

避免：

- 仅仅复述用户说过的话；
- 用抽象标签代替理解；
- 为简单记录制造宏大意义；
- 输出大量正确但无用的套话；
- 为了显得全面而堆砌观点；
- 把所有思考都转化成待办事项。

工具使用

你可以使用以下工具：
- list_topics: 列出用户的所有 Topic（摘要列表）
- read_topic: 获取某个 Topic 的完整详情
- create_topic: 创建一个新 Topic
- update_topic: 更新已有 Topic 的内容

当你需要更新某个 Topic 时，先用 read_topic 获取完整内容，再结合 Record 重新思考后用 update_topic 提交完整的新内容。

输出

完成所有工具调用后，用一段简短的自然语言总结你做了什么：
- 对 Record 的理解
- 采取了什么行动（更新/创建/未操作）
- 原因`;
}
